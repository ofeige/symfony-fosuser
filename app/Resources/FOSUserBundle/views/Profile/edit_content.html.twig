<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                {{ 'profile.edit.submit'|trans({}, 'FOSUserBundle') }}
            </div>

            <div class="panel-body">
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <div>
                        <input class="btn btn-lg btn-success btn-block" type="submit" value="{{ 'profile.edit.submit'|trans({}, 'FOSUserBundle') }}" />
                    </div>
                </form>
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
